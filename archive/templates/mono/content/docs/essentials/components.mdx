---
title: Components
description: The built-in component library for rich, interactive docs.
---

# Components

Beyond Markdown, you get a library of components for richer pages — all available
in any `.mdx` file with no imports.

## Cards

<CardGroup cols={2}>
  <Card title="Fast" icon="zap">
    Static-rendered pages, instant client-side search.
  </Card>
  <Card title="Themeable" icon="palette">
    Restyle everything from one token file.
  </Card>
</CardGroup>

## Callouts

<Note>A neutral note.</Note>
<Tip>A helpful tip.</Tip>
<Warning>Something to be careful about.</Warning>
<Check>A success confirmation.</Check>

## Steps

<Steps>
  <Step title="Write">Add an `.mdx` file under `content/docs`.</Step>
  <Step title="Register">List it in `docs.json`.</Step>
  <Step title="Ship">Push and deploy.</Step>
</Steps>

## Tabs

<Tabs>
  <Tab title="macOS">Press <kbd>⌘</kbd> <kbd>K</kbd> to open search.</Tab>
  <Tab title="Windows">Press <kbd>Ctrl</kbd> <kbd>K</kbd> to open search.</Tab>
</Tabs>

## Accordions

<AccordionGroup>
  <Accordion title="Is it responsive?">Yes — sidebar and TOC collapse on small screens.</Accordion>
  <Accordion title="Does it support dark mode?">Yes — toggle in the top bar, persisted across visits.</Accordion>
</AccordionGroup>

## Parameter fields

Use `<ParamField>` and `<ResponseField>` to document request and response shapes
in prose (the API Reference tab generates these automatically from OpenAPI):

<ParamField query="limit" type="integer" default="20">
  The maximum number of results to return.
</ParamField>

<ResponseField name="id" type="integer" required>
  The unique identifier of the created record.
</ResponseField>
