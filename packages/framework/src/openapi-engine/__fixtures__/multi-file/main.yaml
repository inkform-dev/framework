openapi: 3.1.0
info:
  title: Multi-file Test API
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      summary: List widgets
      tags:
        - widgets
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './widget.yaml#/Widget'
