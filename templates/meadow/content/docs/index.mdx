---
title: Introduction
description: Meadow is a light, amber-toned documentation theme for @inkform/framework — write Markdown, wire up an OpenAPI spec, and ship a fast, searchable docs site.
---

<div className="fw-meadow-hero">
  <h1 className="fw-meadow-hero-title">Meadow Documentation</h1>
  <p className="fw-meadow-hero-subtitle">
    A light, amber-toned starting point for `@inkform/framework` — write
    Markdown, wire up an OpenAPI spec, and ship a fast, searchable docs site.
    No CMS, no database, just your repo.
  </p>
  <a href="/quickstart" className="fw-meadow-hero-cta">
    Get started
    <span className="fw-meadow-hero-cta-arrow" aria-hidden="true">→</span>
  </a>
</div>

## Everything you need to ship docs

A few core building blocks — the rest is just Markdown.

<div className="fw-meadow-feature-grid">
<CardGroup cols={3}>
  <Card>
    <div className="fw-meadow-feature-placeholder" aria-hidden="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.75" strokeLinecap="round" strokeLinejoin="round">
        <path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z" />
        <path d="M14 3v5h5" />
        <path d="M9 13h6" />
        <path d="M9 17h6" />
      </svg>
    </div>
    <p className="fw-meadow-feature-title">Write in MDX</p>
    <p className="fw-meadow-feature-desc">
      Author pages in Markdown with embedded React components — callouts,
      cards, tabs, and steps are all built in.
    </p>
    <div className="fw-meadow-feature-tags">
      <span className="fw-meadow-tag">MDX</span>
      <span className="fw-meadow-tag fw-meadow-tag--accent">Components</span>
    </div>
  </Card>
  <Card>
    <div className="fw-meadow-feature-placeholder" aria-hidden="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.75" strokeLinecap="round" strokeLinejoin="round">
        <path d="m8 6-6 6 6 6" />
        <path d="m16 6 6 6-6 6" />
      </svg>
    </div>
    <p className="fw-meadow-feature-title">Generate a reference</p>
    <p className="fw-meadow-feature-desc">
      Point `docs.json` at an OpenAPI spec and get per-operation pages, code
      samples, and a live Try-It console for free.
    </p>
    <div className="fw-meadow-feature-tags">
      <span className="fw-meadow-tag">OpenAPI</span>
      <span className="fw-meadow-tag fw-meadow-tag--accent">Try It</span>
    </div>
  </Card>
  <Card>
    <div className="fw-meadow-feature-placeholder" aria-hidden="true">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.75" strokeLinecap="round" strokeLinejoin="round">
        <line x1="4" y1="6" x2="20" y2="6" />
        <circle cx="9" cy="6" r="2" />
        <line x1="4" y1="12" x2="20" y2="12" />
        <circle cx="15" cy="12" r="2" />
        <line x1="4" y1="18" x2="20" y2="18" />
        <circle cx="11" cy="18" r="2" />
      </svg>
    </div>
    <p className="fw-meadow-feature-title">Make it yours</p>
    <p className="fw-meadow-feature-desc">
      Recolor, re-font, and restyle the whole site from a single `theme.css`
      token file — no layout code to touch.
    </p>
    <div className="fw-meadow-feature-tags">
      <span className="fw-meadow-tag">Theming</span>
      <span className="fw-meadow-tag fw-meadow-tag--accent">Tokens</span>
    </div>
  </Card>
</CardGroup>
</div>

<Note>
  Ready to dig in? Head to the [Quickstart](/quickstart) — a working site in
  under five minutes.
</Note>
