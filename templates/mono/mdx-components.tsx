import { mdxComponents } from '@freewrite-cms/framework/components';

/**
 * Site-level MDX component map.
 * The framework's defaults cover all built-in blocks (Callout, Card, Steps,
 * Tabs, CodeGroup, Accordion, ParamField, …). Register Aurora-specific
 * overrides here if needed in future iterations.
 */
export const siteMdxComponents = mdxComponents();
